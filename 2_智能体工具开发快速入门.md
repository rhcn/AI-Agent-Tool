智能体工具开发快速入门

```bash
# 安装SDK
conda install pydantic
conda install langchain
conda install langchain-openai
```

**构建一个能够执行加法的智能体工具**


```python
from langchain_openai import ChatOpenAI
from pydantic import SecretStr, BaseModel, Field
from langchain_core.prompts import ChatPromptTemplate
# 假设使用 OpenAI 类 LLM（根据实际 LLM 替换，如 Anthropic、本地化模型等）
from langchain_openai import ChatOpenAI
from langchain_core.tools import tool, Tool

api_key = SecretStr("sk-f6aaecd2fd754755bf4ba3a2bc8a3232")

# 1. 定义工具函数（二选一：要么用 @tool 装饰器，要么用 Tool.from_function）
# 方式一：使用 @tool 装饰器（推荐，更简洁）
class AddInputArgs(BaseModel):
    a: str = Field(description="first number")
    b: str = Field(description="second number")

@tool(
    description="add two numbers",
    args_schema=AddInputArgs,
    return_direct=True,
)

def add(a, b):
    # 注意：参数是 str 类型，需要先转换为数字再计算
    try:
        result = float(a) + float(b)
        # 若为整数，转换为 int 避免 .0 结尾
        return str(int(result) if result.is_integer() else result)
    except ValueError:
        return "错误：请输入有效的数字"

# 方式二：使用 Tool.from_function（如果不用 @tool 装饰器，用这种方式）
# def add(a: str, b: str) -> str:
#     try:
#         result = float(a) + float(b)
#         return str(int(result) if result.is_integer() else result)
#     except ValueError:
#         return "错误：请输入有效的数字"
#
# add_tools = Tool.from_function(
#     func=add,
#     name="add",
#     description="计算两个数相加",
#     args_schema=AddInputArgs  # 绑定参数校验 schema
# )

# 2. 初始化 LLM（根据实际情况替换，这里以 OpenAI 为例）
# 注意：需要设置环境变量 OPENAI_API_KEY，或直接传入 api_key 参数
llm = ChatOpenAI(
    model="qwen-plus",
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
    api_key=api_key,
    streaming=True,
)

# 3. 绑定工具到 LLM（如果用 @tool 装饰器，直接传入函数；如果用 Tool.from_function，传入实例）
llm_with_tools = llm.bind_tools([add])  # 对应方式一
# llm_with_tools = llm.bind_tools([add_tools])  # 对应方式二

# 4. 定义提示词模板
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一名资深的教育专家，擅长回答数学领域的问题。"),
    ("human", "{user_input}"),
])

# 5. 构建链条并执行
chain = prompt | llm_with_tools
response = chain.invoke({"user_input": "计算100+100"})

# 6. 输出结果（处理工具调用的响应）
print(response)

tools_dict = {
    "add": add
}

for tool_calls in response.tool_calls:
    print(tool_calls)

    args = tool_calls['args']
    func_name = tool_calls['name']

    func = tools_dict[func_name]
    tool_content = func.invoke(args)
    print(tool_content)
```

执行结果

```
content='' additional_kwargs={} response_metadata={'finish_reason': 'tool_calls', 'model_name': 'qwen-plus', 'model_provider': 'openai'} id='lc_run--e8cd55c5-8082-4933-af29-9b5be02623fd' tool_calls=[{'name': 'add', 'args': {'a': '100', 'b': '100'}, 'id': 'call_4ed7ac7f9ede4efc8b52bb', 'type': 'tool_call'}]
{'name': 'add', 'args': {'a': '100', 'b': '100'}, 'id': 'call_4ed7ac7f9ede4efc8b52bb', 'type': 'tool_call'}
200
```
